import { cn } from './cn';

// Re-export cn as tw for consistency
export const tw = cn;

// Custom theme colors mapping for NativeWind
export const theme = {
    colors: {
        primary: '#1e3a8a', // blue-900
        secondary: '#2563eb', // blue-600
    },
};